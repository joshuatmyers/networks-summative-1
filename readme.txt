To connect to the server, use the following command:
python client.py "username" "hostname" "port"
-> hostname and port refer to the server, username is your identifier for other people in the server that see your messages

Logging:
The server stores a log file of all messages. This includes info about the time and date the message was sent, alongside the username, port, 
and message that was sent